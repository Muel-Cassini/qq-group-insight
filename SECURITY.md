# 安全说明与审查结论

本项目是一个**只在 `127.0.0.1` 上监听的个人工具**：它读写你本机的聊天导出与文档，并调用你本机的
Edge、Python 与 AI 服务。这份文档说明它的安全模型、审查结论、已知取舍，以及如何报告问题。

---

## 1. 威胁模型

**在范围内的攻击者**

- 你在外面打开的**恶意网页**（可能尝试跨站请求本机的 `127.0.0.1:7801`）；
- **聊天内容 / 上传文档 / 社媒抓回来的文本**里的恶意输入（会流经解析、比对与提示词）；
- **本机其它进程**（同用户权限）——这部分不在防御范围内（它们本来就能读你的文件）。

**不在范围内**

- 取得你 Windows 账户权限的攻击者（DPAPI 与所有本地数据都以当前用户为边界）；
- 目标平台本身的风控与封号（属于使用风险，见 README 的合规章节）。

**明确的非目标**：本项目**没有**鉴权、没有多用户、没有 CSRF token、不做权限分级。它是本机单人工具，
**不要**把它暴露到公网或局域网，也不要改 `host` 去监听 `0.0.0.0`。

---

## 2. 关于 SQL 注入

**结论：自有代码不存在 SQL 注入面。**

理由（可复核）：

- 本项目**不使用任何数据库**。数据流是「JSON/JSONL 文件 → 内存计算 → Markdown 产物」，
  没有 SQL 语句、没有 ORM、没有 `execute()` 调用。
  复核方式：在 `app/`、`.agents/` 下检索 `sqlite|SELECT … FROM|INSERT INTO|UPDATE … SET|DELETE FROM|execute(|cursor(|pymysql|psycopg`
  —— 唯一命中是 `.agents/skills/cross-compare/scripts/compare.py` 的**主题词表**（`mysql`、`postgres`、`sqlite`
  是它用来识别讨论话题的关键词，不是 SQL 代码）。
- 第三方依赖 MediaCrawler 的数据层使用 **SQLAlchemy ORM**（`create_async_engine` / `sessionmaker`）；
  全仓仅 3 处 `text(f"...")` 拼接，且都是**建库 DDL**（`CREATE DATABASE` / `SELECT 1 FROM pg_database`），
  拼接内容是**本地配置文件里的库名**，不来自网络或用户输入。
- 关键词、会话名、文件路径、日期等用户输入全部以**参数或纯文本**参与计算，从不拼进任何查询。

> 结论：把「SQL 注入」当作风险项排查是对的，但在这个架构里它不成立；
> 真正的风险面在**命令行参数**与**文件路径**上（见下）。

---

## 3. 已修复的问题

| # | 问题 | 影响 | 修复 |
|---|---|---|---|
| 1 | **命令行参数注入**：`.cmd`/`.bat` 必须经 `cmd.exe /d /s /c` 调用，而 `cmd` 会对参数**二次解析**（Node 只在参数含空格时才加引号）。DSH 总结的提示词里含用户在 ④ 页填的「风格要求 / 重点关注」自由文本，`openPath` 的 URL 分支也经 `cmd /c start` | 形如 `x&calc&rem` 的输入可执行任意命令（本地权限） | ① DSH 改为 `node <CLI 入口>` 直接 spawn，**完全绕开 shell**；② `openPath` 的 URL 交给 `ShellExecuteW`；③ 兜底路径仍走 `cmd.exe` 时，含 `& \| < > ^ ( ) " % !` 或换行的参数**直接拒绝**（fail closed） |
| 2 | **路径包含判断少了分隔符**：静态资源、产物读取、上传删除都用 `startsWith(root)` 校验 | 同级的 `webXXX` / `artifactsXXX` / `uploadsXXX` 目录会被误判为「在允许范围内」，可被读取或删除 | 统一改为「等于 root 或 `startsWith(root + 分隔符)`」 |
| 3 | ①页「已汇总」判定只按群身份匹配（不看日期范围） | 新导出的记录被旧汇总误标为「已汇总」；配合「隐藏已汇总」会让用户以为记录没被扫到 | 按汇总目录名末尾的日期区间做**覆盖判定** |
| 4 | 「输出目录」允许相对路径 `../` 越界 | 一个手滑把产物写到项目外 | 相对路径必须落在项目内，否则拒绝（绝对路径仍允许） |
| 5 | 前端 `#kvSelectionInfo` 只存在于动态模板 | 手打关键词时报 `Cannot set properties of null` | 移入静态 HTML + 空值兜底 |

以上修复均已实测（修复细节与验证方式见 `HANDOFF.md`）。升级到本版本前请留意 1、2 两项属于**行为变更**：
含命令行特殊字符的参数会被拒绝，需要改用普通文本。

---

## 4. 设计上已有的防护

- **密钥**：API Key 用 Windows DPAPI（`CurrentUser`）加密后存 `app/.local/secrets/`，
  界面只显示「已配置 / 未配置」，代码里不解密回显。删除服务会同时删除密钥。
- **发送给 AI 的内容最小化**：只发汇总文本、比对材料、文档抽取文本；不发原始聊天记录文件、不发图片二进制。
  API 路径发送前有预估与确认弹窗（`confirmBeforeSend`）。
- **本地解析不联网**：关键词提取、文档抽文本、登录判定都在本机 Python 完成。
- **登录态**：只判断 cookie 是否存在**与页面/接口是否真的已登录**，绝不读取、打印或上传 cookie 值。
- **上传**：单文件 40MB 上限；危险后缀（`.exe/.dll/.bat/.cmd/.ps1/.scr/.msi/.vbs/.js/.jar`）被拒绝；
  文件名经 `path.basename` 处理，写入固定批次目录，避免路径穿越。
- **回收站删除**：删除导出走系统回收站（可恢复），不是直接抹除。
- **命令调用**：除上述已修复的点外，所有子进程都用 `spawn(command, args[])`（不拼 shell 字符串）；
  需要 PowerShell 时也把外部数据通过**环境变量或 stdin** 传入，而不是拼进脚本。

---

## 5. 已知取舍（有意接受的风险）

| 项 | 说明 | 建议 |
|---|---|---|
| **无鉴权 / 无 CSRF token** | 服务只监听 `127.0.0.1`。浏览器对 JSON 请求会先发预检（服务未开放 CORS，因此跨站读取与多数跨站写被挡住），但**不带请求体的接口**理论上可被恶意页面触发 | 不要把服务暴露到公网；改端口不改绑定地址；不用时关掉控制台 |
| **任意路径打开 / 目录浏览** | ⑥设置的「浏览…」与 `/api/open` 可以打开任意本机路径，这是刻意设计的（要读用户自定义的导出目录） | 只在自己机器上用 |
| **上传后缀用黑名单** | `.mjs`、`.svg`、`.html` 等不在黑名单里；但这些文件只写入 `artifacts/uploads/`，**不会被当作网页内容提供**，仅作为文本抽取输入 | 不把 `artifacts/` 目录当静态站点发布 |
| **提示词注入（Prompt Injection）** | 群聊文本与社媒内容会进入交给大模型的材料里，理论上可影响模型输出 | 汇总产物只作参考，重要结论回到原文核对 |
| **AI 服务的数据出境** | 用「我的 API」时，材料会发送到你配置的服务商 | 敏感材料改用本地 Ollama / 本地 DSH，或在设置里保持「发送前确认」开启 |
| **`_recon/` 开发脚本** | 本地开发用的侦察 / 验证脚本含绝对路径与一次性逻辑，不随仓库发布 | 需要时自行编写 |

---

## 6. 安全使用建议

1. 控制台只在需要时开启，用完关闭；**不要**修改绑定地址为 `0.0.0.0`。
2. 社媒采集使用**小号**，控制频率；出现验证码/风控就停止。
3. `data/`、`artifacts/`、`app/.local/` 都不要提交到公开仓库或上传网盘。
4. 不要把真实聊天记录粘贴进公开 Issue；报告问题时请脱敏。
5. 定期更新依赖（`pip install -U -r requirements.txt`），尤其是解析文档用的库。

---

## 7. 报告安全问题

请**不要**用公开 Issue 报告漏洞。改用仓库的 **Security → Report a vulnerability**
（GitHub 私有漏洞报告），或在 Issue 里先说明「需要私下沟通」而不贴细节。

请在报告里包含：复现步骤、影响范围、你运行的版本（`app/package.json` 里的版本号）、
以及能证明问题的最少信息。**请勿附带真实聊天记录、昵称或任何 cookie 内容。**
