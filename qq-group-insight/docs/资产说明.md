# 本工作区资产清单

> 记录"现在到底装了什么、有什么数据、有什么产物"，避免重复安装或误删。
> 最后核对时间：2026-09-12（整理目录当天）。

---

## 一、已安装的软件与组件

| 资产 | 位置 | 版本 / 说明 |
|---|---|---|
| Python 虚拟环境 | `.venv\`（576 MB） | Python 3.14.7；MediaCrawler 全部依赖已装齐（含 playwright/opencv/pandas/matplotlib） |
| MediaCrawler（采集后端） | `tools\MediaCrawler\` | 上游 `NanmiCoder/MediaCrawler`（64.9k★）；**NON-COMMERCIAL LEARNING LICENSE 1.1，禁商用** |
| CDP 指向 Edge 的补丁 | `tools\MediaCrawler\sitecustomize.py` | 不改上游源码，通过环境变量把 CDP 浏览器指定为 Edge |
| Edge 调试启动脚本 | `tools\launch-edge-cdp.ps1` | 启动/关闭带 `--remote-debugging-port=9222` 的 Edge |
| QQ Chat Exporter | `%LOCALAPPDATA%\QQChatExporter\` | v6.3.0（GPL-3.0），服务端口 40653，界面 http://localhost:40653/qce |
| Microsoft Edge | `C:\Program Files (x86)\Microsoft\Edge\Application\` | 153.0.4234.32；调试 profile 在 `%LOCALAPPDATA%\dsh-edge-cdp` |

**三个自研技能**（`.agents\skills\`，每个都是 `SKILL.md` + `GUIDE.md` + `config.json` + `scripts\`）：

| 技能 | 作用 | 状态 |
|---|---|---|
| `qq-group-summary` | QQ 聊天记录解析 → 分类/重要度/按天汇总 | 已在 1778 条真实消息上验证 |
| `social-crawl` | 小红书/知乎/B站 只读采集（走 Edge 登录态） | 三平台均已验证可取值 |
| `cross-compare` | 群聊 × 社媒交叉比对材料 | 已在真实数据上验证（9 组配对 / 13 个共同主题） |

---

## 二、已有数据（`data\`）

| 数据 | 位置 | 规模 |
|---|---|---|
| 群聊导出（手动） | `data\qq-chat-exporter\exports\` | 群 **示例群A**（群号 1011090214）1.14 MB JSON + `resources\images\` 121 张图片 |
| 群聊导出（定时） | `data\qq-chat-exporter\scheduled-exports\` | 尚未创建（需要在 QCE 里建定时任务） |

导出覆盖时间：**2026-09-09 ~ 2026-09-12**，共 1778 条消息（其中文本 1518、图片 188、系统提示 70、转发 2）。

---

## 三、已有产物（`artifacts\`，可随时删除后重跑）

| 产物 | 位置 | 内容 |
|---|---|---|
| 群聊汇总 | `artifacts\qq\示例群A-20260909-20260912\` | 4 个按天 digest、`p1-todo.md`、`media-index.md`（122 行）、`normalized.jsonl`（969 KB）、`_report.md` |
| 社媒登录核验采集 | `artifacts\social\2026-09-12-登录核验\` | 小红书 20 条 / 知乎 171 条 / B站 5 条（真实采集，用作链路验证） |
| 合成示例数据 | `artifacts\_samples\` | 9 条假消息 + 4 篇假社媒内容（自检用，非真实数据） |

---

## 四、可以安全删除的内容

| 目录/文件 | 说明 |
|---|---|
| `artifacts\**`（保留 `_samples`） | 全部可由 `data\` 重新生成 |
| `_recon\`（68.7 MB） | 安装期调研缓存：上游 README/LICENSE、GitHub 检索脚本、tarball 解压残留 |
| `.tmp\` | 安装期临时文件 |
| `reference\（已移除的历史项目）\node_modules\` | 第三方教程笔记的依赖，与本流程无关 |

**不要删**：`.agents\`（技能）、`tools\`（采集后端）、`.venv\`（Python 环境）、`data\`（真实聊天数据，删了要重新导出）。

---

## 五、隐私与许可提醒

- `data\` 与 `artifacts\` 含真实群成员昵称、聊天正文、图片，**不要提交到公开仓库或上传网盘**；
- MediaCrawler 限学习研究、禁止商用；QCE 为 GPL-3.0；
- 采集只针对公开内容，建议使用小号并控制频率，避免账号风控。
