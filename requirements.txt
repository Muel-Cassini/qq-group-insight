# 本仓库自有代码（控制台 + 技能）的直接依赖
# 安装：.\.venv\Scripts\python.exe -m pip install -r requirements.txt
#
# 注意：这些是「本地解析/汇总」用的库，全部在本机运行、不联网。
# 版本号为开发时实测通过的版本，写为 >= 以便获得安全修复。

# 文档抽文本（app/server/tools/extract_files.py）
pypdf>=5.0
python-docx>=1.1
beautifulsoup4>=4.12
openpyxl>=3.1
pandas>=2.0
Pillow>=10.0

# 关键词提取与本地汇总（extract_keywords.py / summarize_local.py）
jieba>=0.42

# 可选：解析 .pptx（不安装时遇到 pptx 会给出明确报错，不影响其它格式）
# python-pptx>=0.6.23

# 注意：社媒采集后端 MediaCrawler 不在本仓库内，它有自己的一套依赖：
#   git clone https://github.com/NanmiCoder/MediaCrawler tools/MediaCrawler
#   .\.venv\Scripts\python.exe -m pip install -r tools\MediaCrawler\requirements.txt
